# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Regenerable artifacts. CSV/JSON snapshots are tracked so the skill works
# the instant someone clones it; the 8MB scraped HTML and plot output are
# not — they rebuild trivially.
**/artifacts/models.html
**/artifacts/pareto.png
**/artifacts/pareto_free.png
.claude/
